# Stage 30 query enrichment profiles (ADR-30.1, spec: Query Enrichment).
#
# Deterministic, category-level retrieval terms appended to the base query for a single
# target bucket. Terms are business categories traceable to bank-clearing rules and the
# tracked knowledge base, NOT eval case ids, expected chunk ids or eval query sentences:
#   - rule BC-R001 "Clearing single side" (rules/bank_clearing.yaml)
#   - KB section titles: 日切窗口内的清算单边处理 / 非日切窗口单边核查 /
#     查询发起条件 / 查复所需字段 / T+1 补记核查要素
version: 1
profiles:
  - id: bank-clearing-single-side-missing
    scenario_type: BANK_CLEARING
    error_types:
      - SINGLE_SIDE_MISSING
      - CLEARING_SINGLE_SIDE
    exception_branches:
      - BC-R001
    terms:
      - 清算单边
      - 单边缺失
      - 查询查复
      - T+1 补记
